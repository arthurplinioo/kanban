import SwiftUI

extension Color {
    init(hex: String) {
        var hexSanitized = hex.trimmingCharacters(in: .whitespacesAndNewlines)
        hexSanitized = hexSanitized.replacingOccurrences(of: "#", with: "")
        var rgb: UInt64 = 0
        Scanner(string: hexSanitized).scanHexInt64(&rgb)

        let r = Double((rgb & 0xFF0000) >> 16) / 255
        let g = Double((rgb & 0x00FF00) >> 8) / 255
        let b = Double(rgb & 0x0000FF) / 255
        self.init(red: r, green: g, blue: b)
    }

    // Paleta minimalista — poucas cores de destaque sobre neutros.
    static let tfBackground = Color(.systemBackground)
    static let tfSurface = Color(.secondarySystemBackground)
    static let tfAccent = Color(hex: "#4C6FFF")
    static let tfMuted = Color(.secondaryLabel)
}
